{
  "name": "mcp-guard",
  "version": "0.3.0",
  "description": "Validate, test, and security-audit MCP servers and tool schemas",
  "type": "module",
  "bin": {
    "mcp-guard": "dist/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit",
    "fixtures:gen": "tsx scripts/gen-fixtures.ts"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "files": [
    "dist",
    "docs",
    "README.md",
    "LICENSE",
    "RELEASE.md"
  ]
}
